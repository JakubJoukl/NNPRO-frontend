import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#000080FF',
        },
        secondary: {
            main: '#4747d4',
        },
    }
});

export default theme;